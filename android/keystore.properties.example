# Copy to android/keystore.properties (gitignored) or run:
#   npm run cap:setup:android-signing
#
# Paths are relative to the android/ folder.
storeFile=keystore/skywindow-upload.jks
storePassword=your-store-password
keyAlias=skywindow-upload
keyPassword=your-key-password
